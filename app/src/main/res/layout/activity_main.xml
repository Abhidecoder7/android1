<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@android:color/darker_gray">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="20dp"
        android:background="@android:color/white">

        <!-- Show In-App Notification Button -->
        <Button
            android:id="@+id/btnShowInApp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Show In-App Notification"
            android:padding="12dp"
            android:backgroundTint="#009688"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:layout_marginTop="12dp"
            android:contentDescription="Button to show in-app notifications" />

        <!-- Show App Inbox Button -->
        <Button
            android:id="@+id/btn_show_inbox"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Show App Inbox"
            android:padding="12dp"
            android:layout_marginTop="12dp"
            android:backgroundTint="#3F51B5"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:contentDescription="Button to open App Inbox" />

        <!-- Create User Profile Button -->
        <Button
            android:id="@+id/btnCreate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Create User Profile"
            android:padding="12dp"
            android:backgroundTint="#4CAF50"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:layout_marginTop="12dp"
            android:contentDescription="Button to create a user profile" />

        <!-- Update User Profile Button -->
        <Button
            android:id="@+id/btnUpdate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Update Profile"
            android:padding="12dp"
            android:backgroundTint="#FFC107"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:layout_marginTop="12dp"
            android:contentDescription="Button to update the user profile" />

        <!-- Fetch CleverTap Native Display Button -->
        <Button
            android:id="@+id/btn_fetch_native_display"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Fetch Native Display"
            android:padding="12dp"
            android:backgroundTint="#FF9800"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:layout_marginTop="12dp"
            android:contentDescription="Button to fetch CleverTap native display" />

        <!-- Scrollable Container for CleverTap Native Display -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:fillViewport="true"
            android:contentDescription="Native Display Container">

            <LinearLayout
                android:id="@+id/native_display_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@android:color/white"
                android:elevation="2dp"
                android:orientation="vertical"
                android:padding="10dp">

                <!-- Native Display content will be dynamically added here -->
            </LinearLayout>
        </ScrollView>

    </LinearLayout>
</ScrollView>
